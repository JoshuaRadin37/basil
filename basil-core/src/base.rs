use crate::class::Class;
use crate::function::Function;

pub struct Base;

impl Base {
    pub fn base_class(&self) -> Class {

    }

    fn repr_function(&self) -> Function {
        let statements = vec![

        ]
    }
}
